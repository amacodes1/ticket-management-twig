<div class="flex flex-col gap-4 rounded-xl bg-gray-50 dark:bg-gray-800/50 p-6 shadow-sm">
  <p class="text-lg font-medium text-gray-900 dark:text-white">{{ title }}</p>
  <p class="text-4xl font-bold tracking-tight" style="color: {{ color }}">{{ count }}</p>
</div>
